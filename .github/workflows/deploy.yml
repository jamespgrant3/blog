name: deploy
on: [push]
jobs:
  deploy:
    runs-on: ubuntu-latest
    name: blog-deploy
    steps:
      - name: checkout
        uses: actions/checkout@v3
      - name: use node
        uses: actions/setup-node@v3
        with:
          node-version: 18
